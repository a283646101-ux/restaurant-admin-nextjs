我将按照以下步骤完善后台管理系统，并上传到 GitHub 以触发 Vercel 部署。

### 1. 准备工作
- 安装必要依赖：`zod` (用于数据验证)。
- 创建工具库 `lib/utils.ts`，统一 API 响应格式。

### 2. API 模块实现
我将逐个实现以下 API 模块，确保包含完整的 CRUD、权限验证和业务逻辑：

- **用户模块 (`/api/users`)**
  - `GET`: 获取当前用户信息。
  - `PATCH`: 更新用户信息。

- **优惠券模块 (`/api/coupons`)**
  - `GET`: 获取用户优惠券列表。
  - `POST /issue`: 发放优惠券。
  - `POST /use`: 消费优惠券。

- **评价与反馈模块 (`/api/reviews`, `/api/feedback`)**
  - 实现评论发布、列表查询及点赞功能。
  - 实现用户反馈提交及查看公开反馈。

- **拼团模块 (`/api/group-activities`, `/api/group-orders`)**
  - 获取拼团活动列表。
  - 开团、参团及查询我的拼团记录。

- **地址管理模块 (`/api/addresses`)**
  - 增删改查收货地址。

- **分享记录模块 (`/api/share`)**
  - 记录分享行为并处理奖励逻辑。

### 3. 核心逻辑升级
- **订单 API (`/api/orders`)**: 升级创建订单逻辑，加入库存扣减、优惠券核销和积分计算。

### 4. 部署
- 提交所有更改到本地 Git 仓库。
- 推送到 GitHub，自动触发 Vercel 部署。

确认后，我将立即开始执行。